<h1><%= @course.course_name %></h1>

<h2>Students</h2>
<ul>
  <% @course.students.each do |student| %>
  <li><%= link_to "#{student.first_name} #{student.last_name}", student_path(student.id) %></li>
  <% end %>
</ul>

<br>
<br>

<h2>Add Students to this Course</h2>
  <ul>
    <% @student.each do |student| %>
      <%= form_tag add_student_course_path(@course.id, :course_name => @course.course_name), {:method => :put} %>
        <% unless @course.students.include?(student) %>
          <li><%=student.first_name%><%=submit_tag "Add #{student.first_name}", :class => "btn", value: student.id %></li> 
          <% end %>
        <% end %>   
  </ul></br>
<br>
<br>

<% if current_user %>
  <%= link_to 'Edit this Course', edit_course_path(@course.id) %>
<% end %>

<h2><%= link_to 'View all Courses', courses_path %></h2>
<h2><%= link_to 'View all Students', students_path %></h2>